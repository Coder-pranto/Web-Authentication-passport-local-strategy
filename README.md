# Web-Authentication-passport-local-strategy
I've used passport, passport-local (strategy), express-session (for session management), connect-mongo(for separate session storage) pkg.

-check in master branch

